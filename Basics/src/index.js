const template = (
  <div>
    <h1>React Basics</h1>
    <p>React is really Cool.</p>
    <h4>Everything is going smooth.</h4>
  </div>
);
const appRoot = document.getElementById("app");

ReactDOM.render(template, appRoot);
